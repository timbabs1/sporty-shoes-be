spring.application.name=sportyshoes
server.port=8081
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/sporty
spring.datasource.username=root
spring.datasource.password=password
#for cors
cors.allowed.origins=http://localhost:3000,http://localhost:4200
#product.image.upload-dir=file:///Users/babzhaastrup/Documents/Work_Docs_VF_UK/SimpliLearn_Java/sporty-shoes-be/sporty-shoes-images

#for image upload directory
product.image.upload-dir=sporty-shoes-images

#jpa-hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
#converts the pojo into the DDL formated table
#create- if the table is exiting or not existing this command deletes the table and creates a new table
# update - if table exists it doesnt delete the table  / if table doesnt exists it creates it
spring.jpa.hibernate.ddl-auto=update
#sql on my server
spring.jpa.show-sql=true